const year=document.getElementById("year");if(year)year.textContent=new Date().getFullYear();
const serviceNames={software:"Pengembangan Software",website:"Website & Landing Page",api:"API & Backend",integration:"Integrasi Digital",cloud:"Deployment & Infrastruktur",consulting:"Konsultasi Teknologi"};
const serviceSelect=document.getElementById("service"),serviceSummary=document.getElementById("serviceSummary"),orderForm=document.getElementById("orderForm"),success=document.getElementById("success");
if(serviceSelect){const params=new URLSearchParams(location.search),selected=params.get("service");if(selected&&serviceNames[selected])serviceSelect.value=selected;const update=()=>{if(serviceSummary)serviceSummary.textContent=serviceNames[serviceSelect.value]||"Pilih layanan"};serviceSelect.addEventListener("change",update);update()}
if(orderForm){orderForm.addEventListener("submit",e=>{e.preventDefault();const data=new FormData(orderForm),service=serviceNames[data.get("service")]||data.get("service");document.getElementById("successText").textContent="Order siap dikonfirmasi. Layanan: "+service+". Tim akan mengonfirmasi ruang lingkup, harga, jadwal, dan metode pembayaran sebelum transaksi.";success.classList.add("show");orderForm.reset();if(serviceSelect){const params=new URLSearchParams(location.search);serviceSelect.value=params.get("service")||"software";serviceSelect.dispatchEvent(new Event("change"))}})}